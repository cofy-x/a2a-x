{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run current file",
      "type": "shell",
      "command": "npx",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "args": ["tsx", "${file}"],
      "problemMatcher": []
    }
  ]
}
